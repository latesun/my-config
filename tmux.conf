set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-battery'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-net-speed'

# modify prefix
unbind C-b
set -g prefix C-s

# split window short key
bind | split-window -h
bind - split-window -v

# vi-mode jump different window
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Terminal type configuration
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

set-option -g message-command-style fg=yellow,bold,underscore,blink
set-option -g message-command-style bg=black,fg=default,noreverse

set -g @net_speed_interfaces "wlp4s0"

set -g status-left-length 40
set -g status-right-length 200
set -g status-right 'D: #{download_speed} | U: #{upload_speed} | CPU: #{cpu_percentage} | BAT: #{battery_percentage} #{battery_remain} | %a %h-%d %H:%M '

# set -g @plugin 'sei40kr/tmux-airline-dracula'
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
